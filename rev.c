//How many characters are in a string

#include <stdio.h>

#define entry 100
int main(void) {
char entry[], c;
int i=0, j=0;

scanf("Entry(no more than 100 char): %s", &entry);
while ((c = getchar()) != "\n", i++) {
entry[] = c
}
for (j=0, j<i, j++) {
printf("", entry[j]) 

return 0;
}